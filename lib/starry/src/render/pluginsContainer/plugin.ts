export interface IpluginConfig {
    type: string, //npm,local
    path?: string, // local path
    name?: string, // npm name
    isDev?: boolean, //is dev plugin
}

export default class plugin {
    
    constructor() {

    }
}